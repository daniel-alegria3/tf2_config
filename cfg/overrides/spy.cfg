/// Make sure previous slot3 isn't used to make sure lastinv.cfg works
exec scripts/binds.cfg
exec scripts/conciser_disguisekit.cfg
x_lastinv_set_slot3
x_lastinv_set_slot1
resup_spy

/// Binds
bind "q" "+re_use_dteam"
bind "a" "lastdisguise"
bind "b" "disguisekit"

bind "x" "disguisekit_offensive"
bind "c" "disguisekit_defensive"
bind "v" "disguisekit_support"
// bind "space" "+zap"

/// Aliases
alias disguisekit "slot4"
alias +re_use_dteam "+re_use; disguiseteam"
alias -re_use_dteam "-re_use"

// +zap also pulls out the disguise's secondary
// alias +sap "slot2; +attack; lastdisguise"
// alias -sap "-attack; slot1"

